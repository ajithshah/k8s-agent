replicaCount: 1

image:
  repository: ubuntu
  tag: latest
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 50m
    memory: 64Mi
  requests:
    cpu: 20m
    memory: 32Mi

command:
- sleep
- infinity

service:
  type: ClusterIP
  port: 80
  targetPort: 80
