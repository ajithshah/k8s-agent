command:
- sleep
- infinity
image:
  pullPolicy: Always
  repository: ubuntu
  tag: latest
replicaCount: 1
resources:
  limits:
    cpu: 20m
    memory: 256Mi
  requests:
    cpu: 10m
    memory: 128Mi
service:
  port: 80
  targetPort: 80
  type: ClusterIP
