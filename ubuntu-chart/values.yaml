replicaCount: 1

image:
  repository: ubuntu
  tag: 99.99-invalid
  pullPolicy: Always

command:
- sleep
- infinity

resources:
  limits:
    cpu: 20m
    memory: 124Mi
  requests:
    cpu: 10m
    memory: 1Mi

service:
  type: ClusterIP
  port: 80
  targetPort: 80
