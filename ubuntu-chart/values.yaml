replicaCount: 1

image:
  repository: ubuntu
  tag: latest
  pullPolicy: Always

command:
- /bin/bash
- -c
- "exit 1"

resources:
  limits:
    cpu: 20m
    memory: 124Mi
  requests:
    cpu: 10m
    memory: 1Mi

service:
  type: ClusterIP
  port: 80
  targetPort: 80
