replicaCount: 1

image:
  repository: ubuntu
  tag: latest
  pullPolicy: Always

command:
- sleep

resources:
  limits:
    cpu: 20m
    memory: 124Mi
  requests:
    cpu: 10m
    memory: 16Mi

service:
  type: ClusterIP
  port: 80
  targetPort: 80
