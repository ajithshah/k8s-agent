replicaCount: 1

image:
  repository: nginx
  tag: "1.25latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 50m
    memory: 64Mi
  requests:
    cpu: 20m
    memory: 32Mi

service:
  type: ClusterIP
  port: 80
  targetPort: 80
